<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="m">
        <activity
            android:name=".activity.ActivityForLeapYears"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.ActivityForWorkingExperience"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.ActivityForAgeComparison"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.ActivityForRecyclerView"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.ActivityForDetailsOfBirthday"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>